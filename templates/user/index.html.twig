{% extends 'base.html.twig' %}

{% block title %}Utilisateurs{% endblock %}

{% block body %}
    <h2 class="text-4xl font-extrabold dark:text-white">Utilisateurs</h2>
    <div class="divider"></div>
    <a href="{{ url('user_add') }}" class="btn btn-info">Ajouter un utilisateur...</a>
    <div class="divider"></div>
    {% for label, messages in app.flashes(['success', 'danger']) %}
        {% for message in messages %}
            <div class="alert alert-{{ label }}">
                {{ message }}
            </div>
        {% endfor %}
    {% endfor %}
    <ul>
    {% for u in users %}
        <div class="divider"></div>
        <li>{{ u.login }} <a href="{{ url('user_delete',{id: u.id}) }}" class="btn btn-error">Supprimer</a>
            <a href="{{ url('operation_add', {id: u.id}) }}" class="btn btn-warning">Ajouter une Operation...</a>
        </li>
    {% endfor %}
    </ul>
{% endblock %}
